buildscript {
  // Dependency versions
  ext.badassRuntimeVersion="1.13.0"
  ext.cliktVersion="4.2.1"
  ext.gdxVersion="1.12.1"
  ext.jUnitPlatformVersion="1.10.1"
  ext.jUnitVersion="5.10.1"
  ext.klaxonVersion="5.6"
  ext.kotlinCsvVersion="1.9.2"
  ext.kotlinVersion="1.9.22"
  ext.kotlinxCoroutinesVersion="1.7.3"
  ext.ktxVersion="1.12.1-rc1"
  ext.textraTypistVersion="0.10.0"

  repositories {
    mavenCentral()
    maven { url 'https://s01.oss.sonatype.org' }
    gradlePluginPortal()
  }
  dependencies {
    classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"
  }
}

allprojects {
  apply plugin: 'eclipse'
  apply plugin: 'idea'
}

configure(subprojects) {
  apply plugin: 'java-library'
  apply plugin: 'kotlin'
}

subprojects {
  version = '1.0.0'
  ext.appName = 'HybridSim'
  repositories {
    mavenCentral()
    maven { url 'https://s01.oss.sonatype.org' }
    gradlePluginPortal()
  }
}

eclipse.project.name = 'HybridSim' + '-parent'
